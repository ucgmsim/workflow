[[root]]
        platform = mahuika-slurm
        pre-script = """
        module load Apptainer
        """
        [[[directives]]]
                --account = nesi00213
        [[[environment]]]
                {% for var, value in environment.items() -%}
                {{var}} = "{{value}}"
                {% endfor -%}

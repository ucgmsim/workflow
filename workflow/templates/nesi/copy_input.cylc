[[copy_input]]
        platform = localhost
        script = "[[ ! -d $CYLC_WORKFLOW_RUN_DIR/input ]] || [[ -z \"$( ls -A '$CYLC_WORKFLOW_RUN_DIR/input' )\" ]] || cp -r $CYLC_WORKFLOW_RUN_DIR/input/* $CYLC_WORKFLOW_SHARE_DIR/"

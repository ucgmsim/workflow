[[{{stage_node_identifier(stage)}}]]
        script = apptainer exec -c --bind "$CYLC_WORKFLOW_SHARE_DIR/.cache:/cache:$PWD:/out,$CYLC_WORKFLOW_SHARE_DIR:/share/{{realisation_identifier(stage)}}" merge-ts /share/LF/OutBin /share/LF/OutBin/output.e3d

[[{{stage_node_identifier(stage)}}]]
    script = apptainer exec -c --bind "$PWD:/out,$CYLC_WORKFLOW_SHARE_DIR/{{realisation_identifier(stage)}}:/share" {{container}} generate-station-coordinates /share/realisation.json /share/stations --stat-file /stations.ll

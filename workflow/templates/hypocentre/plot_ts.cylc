[[{{stage_node_identifier(stage)}}]]
        script = apptainer exec -c --bind "$PWD:/out,$CYLC_WORKFLOW_SHARE_DIR:/share/{{realisation_identifier(stage)}}" plot-ts /share/LF/OutBin/output.e3d /share/animation.mp4

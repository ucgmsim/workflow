[[{{stage_node_identifier(stage)}}]]
        script = apptainer exec -c --bind "$PWD:/out,$CYLC_WORKFLOW_SHARE_DIR/{{realisation_identifier(stage)}}:/share" {{container}} nshm2022-to-realisation /nshmdb.db {{stage.event}} /share/realisation.json {{defaults_version}}

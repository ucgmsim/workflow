[[{{stage_node_identifier(stage)}}]]
        script = mpirun --use-hwthread-cpus -n 48 {{emod3d_path}} -args "par=$CYLC_WORKFLOW_SHARE_DIR/{{realisation_identifier(stage)}}/LF/e3d.par"
        inherit = large_job
